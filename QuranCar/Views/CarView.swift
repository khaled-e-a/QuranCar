import SwiftUI
import QuranKit

struct CarView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("Car Mode")
                    .font(.title)

                // Add your car mode interface here
                // This should be optimized for use while driving

                Spacer()
            }
        }
    }
}